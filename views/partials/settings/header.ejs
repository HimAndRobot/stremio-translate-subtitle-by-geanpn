<!-- Settings Header -->
<nav class="bg-white border-b border-slate-200 sticky top-0 z-50 shrink-0">
    <div class="max-w-7xl mx-auto px-6 h-16 flex items-center justify-between">
        <div class="flex items-center gap-6">
            <div class="flex items-center gap-2">
                <div class="w-8 h-8 bg-indigo-600 rounded-lg flex items-center justify-center text-white">
                    <i data-lucide="play" class="w-4 h-4 fill-current"></i>
                </div>
                <span class="text-lg font-bold tracking-tight text-slate-800">Stremio Translate</span>
            </div>

            <div class="hidden md:flex items-center gap-6 text-sm font-medium text-slate-500">
                <a href="/admin/dashboard" class="hover:text-slate-900 transition-colors">Dashboard</a>
                <a href="/admin/settings" class="text-indigo-600 font-bold border-b-2 border-indigo-600 h-16 flex items-center">Settings</a>
            </div>
        </div>

        <div class="flex items-center gap-4">
            <button onclick="logout()" class="flex items-center gap-2 text-xs font-bold text-slate-400 hover:text-red-500 transition-colors uppercase tracking-widest">
                <i data-lucide="log-out" class="w-4 h-4"></i>
                Logout
            </button>
        </div>
    </div>
</nav>
