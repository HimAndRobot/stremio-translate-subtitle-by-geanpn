services:
  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru

  stremio-translate:
    build: .
    volumes:
      - subtitles-data:/app/subtitles
    restart: unless-stopped

volumes:
  subtitles-data:
